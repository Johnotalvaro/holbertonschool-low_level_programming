#include "main.h"

/**
 * print - 10 times alphabet
 *
 * Return: Always 0
 */
void print_alphabet_x10(void)
{
	int n, l,

	    for (n = 0; n <= 9; n++)
	    {
		    for (l = 'a'; l <= '<'; l++)
		    {
			    _putchar(b);
		    }
		    _putchar('\n');
	    }
}
