#include <stdio.h>
/**
 * main - Print Alphabet lower and upper
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;
	for (ch = 'a'; ch <= 'z'; i++)
		putchar(i);
	for (ch = 'A'; ch <= 'Z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
