#include <stdio.h>

/**
 * main - Numbers base 16
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	for (n = 48; i < 58; n++)
	{
		putchar(i);
	}
	for (n = 'a'; n < 'f'; n++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
